{
  "hash": "40488eabc9070271fc17adb66b908c48",
  "result": {
    "engine": "knitr",
    "markdown": "# Introduction to Data Visualization {#week7}\n\n::: {.callout-tip}\n## What This Session Covers\n\nThis session introduces data visualization with ggplot2 - how to build informative, publication-ready graphs layer by layer.\n\n1. **The ggplot framework** - the grammar of graphics: data, aesthetics, geoms, layers\n2. **Basic principles** - what makes a visualization honest and informative\n3. **Building graphs** - scatterplots, line graphs, histograms, bar plots, and boxplots\n4. **Customizing** - axes, titles, symbols, text annotations, themes, and legends\n:::\n\nThis week, we focus on data visualization. And the aim of this week is to discuss some basic principles of data visualization as well as showcasing how to visualize data using R. \n\n\nWe have already encountered data visualization in week 4 when we visualized scores form different language classes. In that session, we used the `plot` function to visualize the data as a scatter plot (as shown below).\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(here)\ndat <- read.delim(here(\"data\", \"week4data1.txt\"), sep = \"\\t\")\nplot(dat$TestScores,\n     col = factor(dat$Class))\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/prep-1.png){width=672}\n:::\n:::\n\n\n\n\nToday, we will go beyond the `plot` function and us the `ggplot` function instead.\n\nTo be able to generate `ggplot` plots, you need to install and load the `ggplot2` package. We also install and load the `gridExtra` package (for arranging several plots in one panel) and the `dplyr` package.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# install packages\n#install.packages(\"ggplot2\")\n#install.packages(\"gridExtra\")\n#install.packages(\"dplyr\")\n# load packages\nlibrary(ggplot2)\nlibrary(gridExtra)\nlibrary(dplyr)\n```\n:::\n\n\n\n\n\n## The ggplot framework\n\n::: {.callout-note}\n## Key concept: The grammar of graphics\n\nggplot2 builds every plot from the same three ingredients: **data** (a table), **aesthetic mappings** (`aes()` - which variables map onto which visual properties: x, y, color, shape, size), and **geoms** (the geometric objects that display the data: `geom_point()`, `geom_line()`, `geom_boxplot()`, ...). Layers are added with `+`. Once you understand this grammar, every new plot type is just a new combination of the same building blocks - not a new function to memorize.\n:::\n\n\nThe philosophy of *ggplot2* is to consider graphics as consisting out of elements called *aesthetics* (which include, e.g., the dataset to be plotted and the axes) and layers called *geometrics* containing information (e.g., points or lines).\n\nThe underlying principle is that a visualization is build up by adding layers as shown below. \n\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![](07-week7_files/figure-html/plot10-1.png){width=672}\n:::\n:::\n\n\n\n\nThus, ggplots typically start with the function call (`ggplot`) followed by the specification of the data, then the aesthetics (`aes`), and then a specification of the type of plot that is created (`geom_line` for line graphs, `geom_box` for box plots, `geom_bar` for bar graphs, `geom_text` for text, etc.). \n\n> `ggplot(data, aes(x, y)) + geom_point()`\n\nThe nice thing about ggplots is that they allow to specify all elements that the graph consists of (e.g. colors, symbols, text, themes, axes, labels, etc.). \n\nLet's see how this works in practice and visualize the week 4 dataset.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(ggplot2)\nggplot(dat, aes(x = Subject, y = TestScores, color = Class)) +\n         geom_point()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/p1-1.png){width=672}\n:::\n:::\n\n\n\n\n\nAs the ggplot framework has become the dominant way to create visualizations in R, we will only focus on this framework in the following practical examples.\n\nBut before we plot data, let us think about some basic principles of data visualization.\n\n## Basic principles of data vis\n\nOn a very general level, graphs should be used to inform the reader about properties and relationships between variables. This implies that...\n\n- graphs, including axes, must be labeled properly to allow the reader to understand the visualization correctly and with ease.  \n- there should not be more dimensions in the visualization than there are in the data (**unless** it helps the reader to better understand or more easily interpret information shown by the visualization).  \n- all elements within a graph should be unambiguous.  \n- variable scales should be portrayed accurately (for instance, lines - which imply continuity - should not be used for categorically scaled variables).  \n- graphs should be as intuitive as possible and should not mislead the reader.\n\n\n## Creating a simple graph\n\nWhen creating a visualization with ggplot, we first use the function `ggplot` and define the data that the visualization will use, then, we define the aesthetics which define the layout, i.e. the x- and y-axes. \n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores))\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/plot1-1.png){width=672}\n:::\n:::\n\n\n\n\nIn a next step, we add the geom-layer which defines the type of visualization that we want to display. In this case, we use `geom_point` as we want to show points that stand for the test scores for each subject. Note that we add the geom-layer by adding a `+` at the end of the line!\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores)) +\n  geom_point()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/plot2-1.png){width=672}\n:::\n:::\n\n\n\n\nWe can also add another layer, e.g. a layer which shows a smoothed loess line, and we can change the theme by specifying the theme we want to use. Here, we will use `theme_bw` which stands for the black-and-white theme (we will get into the different types of themes later).\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores)) +\n  geom_point() +\n  geom_smooth() +\n  theme_bw()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/plot3-1.png){width=672}\n:::\n\n```{.r .cell-code}\n# save plot\nggsave(here(\"images\", \"myplot.png\"))\n```\n:::\n\n\n\n\nTo save a plot, we use the `ggsave` function after the plot as used above.\n\n\nAnd we can store plots in objects.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores)) +\n  geom_point() +\n  theme_bw() -> p\n```\n:::\n\n\n\n\n\n\n\nTo change the range of the axes, we can specify their limits in the `coord_cartesian` layer.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\np + coord_cartesian(xlim = c(0, 55), ylim = c(0, 50))\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/axes2-1.png){width=672}\n:::\n:::\n\n\n\n\nWe can also integrate plots into data processing pipelines as shown below. When you integrate visualizations into pipelines, you should not specify the data as it is clear from the pipe which data the plot is using.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndat |>\n  group_by(Class) |>\n  dplyr::summarise(Score = mean(TestScores)) |>\n    ggplot(aes(x = Class, y = Score, \n               fill = Class)) +\n    geom_bar(stat = \"identity\")\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/plot5-1.png){width=672}\n:::\n:::\n\n\n\n\n## Modifying axes and titles\n\nThere are different way to modify axes, the easiest way is to specify the axes labels using `labs` (as already shown above). To add a custom title, we can use `ggtitle`.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\np + labs(x = \"Student\", y = \"Test Score (out of 50)\") +\n  ggtitle(\"Test results of students in language classes\", subtitle=\"The students performed really badly!\")\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/axes1-1.png){width=672}\n:::\n:::\n\n\n\n\nTo change the range of the axes, we can specify their limits in the `coord_cartesian` layer.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\np + coord_cartesian(xlim = c(0, 55), ylim = c(0, 50))\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/axes3-1.png){width=672}\n:::\n:::\n\n\n\n\n\n## Line Graphs\n\nLine graphs are used when we have numeric values that are linked in one way or another, e.g., because they display a time series (in our case we should actually not use a line graph but we do it to exemplify how it is done). \n\n> It is important  that there is only one value per x-axis level for each line! \n\nIn addition, we use the `facet_wrap` function with the `nrow` argument to show the plots in 3 rows (instead of next to each other). \n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndat |>\n  ggplot(aes(x = Subject, y = TestScores, color = Class)) +\n  # add geom layer with lines\n  geom_line() +\n  facet_wrap(~Class, nrow = 3)\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/line1-1.png){width=672}\n:::\n:::\n\n\n\n\n\n\n## Histograms\n\nHistograms summarize numeric variables by showing their distribution across bins. \n\n\nUsing `ggplot`, we specify the variable we want to summarize in the aesthetics and use the `geom_histogram` function to generate a histogram. And we can simply add information about a second variable by specifying this variable as the basis for the coloring of the bars (which we do by specify the `fill` argument). \n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(TestScores, fill = Class)) +\n  geom_histogram()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/hist_03-1.png){width=672}\n:::\n:::\n\n\n\n\n## Bar plots\n\n\n\nThe creation of barplots in ggplot works just like other types of visualizations in this framework. We first define the data and the aesthetics and then use the `geom_bar` to create a barplot.\n\nIn the case of barplots, it is important that we only have 1 value per class so we first summarize the data before plotting it.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# bar plot\ndat |>\n  dplyr::group_by(Class) |>\n  dplyr::summarise(Score = mean(TestScores)) |>\n  ggplot(aes(x = reorder(Class, Score), y = Score, fill = Class)) +\n  geom_bar(stat=\"identity\") +          \n  theme_bw() +                        \n  # add and define text\n  geom_text(aes(y = Score-2, label = round(Score, 1)), color = \"white\", size=5) + \n  # add colors\n  scale_fill_manual(values = c(\"gray\", \"orange\", \"lightblue\")) +\n  # suppress legend\n  theme(legend.position=\"none\")\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/bar4-1.png){width=672}\n:::\n:::\n\n\n\n\nCompared with the pie chart, it is much easier to grasp the relative size and order of the percentage values which shows that pie charts are unfit to show relationships between elements in a graph and, as a general rule of thumb, should be avoided.\n\n## Boxplots\n\nSo far, we have plotted values but we have not plotted the underlying distributions. For instance, we have plotted mean values but not the variance within the distribution. One handy way to combine plotting general trends and their underlying distributions are boxplots.\n\nBoxplots, or Box-and-Whisker Plots, are exploratory graphics first created by John W. Tukey and they show the relationships between categorical and numeric variables. They are very useful because they not only provide measures of central tendency (the median which is the line in the middle of the box) but they also offer information about the distribution of the data. To elaborate, fifty percent of data points fall within the box while seventy-five percent of data points fall within the whiskers (the lines which look like extended error bars): the box thus encompasses the interquartile range between the first and third quartile. The whiskers show the minimum and maximum values in the data and only outliers (data points that lie 1.5 times the interquartile range or more above the third quartile or 1.5 times the interquartile range or more below the first quartile. If the whiskers differ in length, then this means that the data is asymmetrically distributed.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# create boxplot\nggplot(dat, aes(Class, TestScores, color = Class)) +                 \n  geom_boxplot(fill=c(\"orange\", \"gray\", \"lightblue\")) \n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/box1-1.png){width=672}\n:::\n:::\n\n\n\n\n\n\n\n## Modifying symbols and adding text\n\nTo change the symbols that the plotted, we modify the `shape` argument.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores, shape = Class, color = Class)) +\n  geom_point() \n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/shape1-1.png){width=672}\n:::\n:::\n\n\n\n\nText can be added by adding a text layer.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndat |>\n  ggplot(aes(x = Subject, y = TestScores, label = TestScores, color = Class)) +\n  geom_text(size = 3) +\n  theme_bw()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/text1-1.png){width=672}\n:::\n:::\n\n\n\n\n\nOr, we can use the annotate layer.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores)) +\n  geom_point() +\n  ggplot2::annotate(geom = \"text\", label = \"Some text\", x = 10, y = 25, color = \"orange\") +\n  ggplot2::annotate(geom = \"text\", label = \"More text\", x = 200, y = 35, color = \"lightblue\", size = 8) +\n    theme_bw()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/text5-1.png){width=672}\n:::\n:::\n\n\n\n\n\nText is also useful when generating bar plots to indicate the values for different columns.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndat |>\n  dplyr::group_by(Class) |>\n  dplyr::summarise(Score = round(mean(TestScores), 1)) |>\n  ggplot(aes(x = Class, y = Score, label = Score)) +\n  geom_bar(stat=\"identity\") +\n  geom_text(vjust=-1.6, color = \"black\") +\n  coord_cartesian(ylim = c(0, 50)) +\n  theme_bw()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/text6-1.png){width=672}\n:::\n:::\n\n\n\n\n\nTo show multiple plots in one window, we can create facet grids (here we want to have separate plots for each Class).\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores)) +\n  facet_grid(~Class) +\n  geom_point() + \n  theme_bw()\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/combine1-1.png){width=672}\n:::\n:::\n\n\n\n\n\n## Modifying themes and legends\n\nTo change the legend position, we specify the `legend.position` in the `theme` layer.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores, color = Class)) +\n  geom_point() +\n  # use black-and-white theme\n  theme_bw() +\n  theme(legend.position = \"top\")\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/legend1-1.png){width=672}\n:::\n:::\n\n\n\n\nWe can remove legends by specifying the position as `none`.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(dat, aes(x = Subject, y = TestScores, color = Class)) +\n  geom_point() + \n  # use void theme\n  theme_void() +\n  theme(legend.position = \"none\")\n```\n\n::: {.cell-output-display}\n![](07-week7_files/figure-html/legend2-1.png){width=672}\n:::\n:::\n\n\n\n\n\n\n\n::: {.callout-tip}\n## Exercises: Data Visualization\n:::\n\n**Q1. In ggplot2, what is the role of aes()?**\n\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n```{=html}\n<form name=\"form_W7_Q1\" onsubmit=\"return validate_form_W7_Q1()\" method=\"post\">\n<label>\n<input type=\"radio\" name=\"answer_W7_Q1\" id=\"answer_W7_Q1_1\" value=\"It saves the plot to a file\"/>\nIt saves the plot to a file\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q1\" id=\"answer_W7_Q1_2\" value=\"It filters the data before plotting\"/>\nIt filters the data before plotting\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q1\" id=\"answer_W7_Q1_3\" value=\"It chooses the color palette of the plot\"/>\nIt chooses the color palette of the plot\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q1\" id=\"answer_W7_Q1_4\" value=\"It maps variables in the data onto visual properties such as x, y, and color\"/>\nIt maps variables in the data onto visual properties such as x, y, and color\n</label>\n<br/>\n<input type=\"submit\" value=\"Check answer\"/>\n<div id=\"result_W7_Q1\"></div>\n</form>\n<script>function validate_form_W7_Q1() {var x, text; var x = document.forms['form_W7_Q1']['answer_W7_Q1'].value;if (x == 'It maps variables in the data onto visual properties such as x, y, and color'){text = 'Correct! aes() defines the aesthetic mappings - which variable determines position on the x-axis, y-axis, the coloring, and so on. The geoms then draw those mappings.';} else {text = 'aes() connects <em>variables</em> to <em>visual properties</em> - it does not itself draw or style anything.';} document.getElementById('result_W7_Q1').innerHTML = text; evaluate_final_score(); return false;}function evaluate_final_score(){\n         element = document.getElementById('checkdown_final_score');\n         if(element === null){return false;} else {var element, text, res1;res1 = document.getElementById('result_W7_Q1').innerText == 'Correct! aes() defines the aesthetic mappings - which variable determines position on the x-axis, y-axis, the coloring, and so on. The geoms then draw those mappings.';text = res1;element.innerHTML = text;\n         return false;\n         }}</script>\n```\n\n:::\n:::\n\n\n\n\n---\n\n**Q2. You want to display the distribution of a single numeric variable (e.g. essay lengths). Which geom is most appropriate?**\n\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n```{=html}\n<form name=\"form_W7_Q2\" onsubmit=\"return validate_form_W7_Q2()\" method=\"post\">\n<label>\n<input type=\"radio\" name=\"answer_W7_Q2\" id=\"answer_W7_Q2_1\" value=\"geom_bar()\"/>\ngeom_bar()\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q2\" id=\"answer_W7_Q2_2\" value=\"geom_line()\"/>\ngeom_line()\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q2\" id=\"answer_W7_Q2_3\" value=\"geom_histogram()\"/>\ngeom_histogram()\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q2\" id=\"answer_W7_Q2_4\" value=\"geom_point()\"/>\ngeom_point()\n</label>\n<br/>\n<input type=\"submit\" value=\"Check answer\"/>\n<div id=\"result_W7_Q2\"></div>\n</form>\n<script>function validate_form_W7_Q2() {var x, text; var x = document.forms['form_W7_Q2']['answer_W7_Q2'].value;if (x == 'geom_histogram()'){text = 'Correct! Histograms bin a continuous variable and show how many observations fall into each bin, revealing the shape of the distribution. geom_bar() is for counting categories, not binning continuous values.';} else {text = 'You need a geom that bins a <em>continuous</em> variable and displays frequencies of the bins.';} document.getElementById('result_W7_Q2').innerHTML = text; evaluate_final_score(); return false;}function evaluate_final_score(){\n         element = document.getElementById('checkdown_final_score');\n         if(element === null){return false;} else {var element, text, res1, res2;res1 = document.getElementById('result_W7_Q1').innerText == 'Correct! aes() defines the aesthetic mappings - which variable determines position on the x-axis, y-axis, the coloring, and so on. The geoms then draw those mappings.'; res2 = document.getElementById('result_W7_Q2').innerText == 'Correct! Histograms bin a continuous variable and show how many observations fall into each bin, revealing the shape of the distribution. geom_bar() is for counting categories, not binning continuous values.';text = res1 + res2;element.innerHTML = text;\n         return false;\n         }}</script>\n```\n\n:::\n:::\n\n\n\n\n---\n\n**Q3. What does the box in a boxplot represent?**\n\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n```{=html}\n<form name=\"form_W7_Q3\" onsubmit=\"return validate_form_W7_Q3()\" method=\"post\">\n<label>\n<input type=\"radio\" name=\"answer_W7_Q3\" id=\"answer_W7_Q3_1\" value=\"The mean plus and minus one standard deviation\"/>\nThe mean plus and minus one standard deviation\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q3\" id=\"answer_W7_Q3_2\" value=\"The interquartile range - the middle 50% of the data, with the median as a line inside the box\"/>\nThe interquartile range - the middle 50% of the data, with the median as a line inside the box\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q3\" id=\"answer_W7_Q3_3\" value=\"The full range from minimum to maximum\"/>\nThe full range from minimum to maximum\n</label>\n<br/>\n<label>\n<input type=\"radio\" name=\"answer_W7_Q3\" id=\"answer_W7_Q3_4\" value=\"The 95% confidence interval of the mean\"/>\nThe 95% confidence interval of the mean\n</label>\n<br/>\n<input type=\"submit\" value=\"Check answer\"/>\n<div id=\"result_W7_Q3\"></div>\n</form>\n<script>function validate_form_W7_Q3() {var x, text; var x = document.forms['form_W7_Q3']['answer_W7_Q3'].value;if (x == 'The interquartile range - the middle 50% of the data, with the median as a line inside the box'){text = 'Correct! The box spans from the 1st quartile (25th percentile) to the 3rd quartile (75th percentile), so it contains the middle half of the data. The line inside is the median; the whiskers extend to the most extreme values within 1.5 IQR, and points beyond are plotted as potential outliers.';} else {text = 'The box shows quartiles, not means or standard deviations.';} document.getElementById('result_W7_Q3').innerHTML = text; evaluate_final_score(); return false;}function evaluate_final_score(){\n         element = document.getElementById('checkdown_final_score');\n         if(element === null){return false;} else {var element, text, res1, res2, res3;res1 = document.getElementById('result_W7_Q1').innerText == 'Correct! aes() defines the aesthetic mappings - which variable determines position on the x-axis, y-axis, the coloring, and so on. The geoms then draw those mappings.'; res2 = document.getElementById('result_W7_Q2').innerText == 'Correct! Histograms bin a continuous variable and show how many observations fall into each bin, revealing the shape of the distribution. geom_bar() is for counting categories, not binning continuous values.'; res3 = document.getElementById('result_W7_Q3').innerText == 'Correct! The box spans from the 1st quartile (25th percentile) to the 3rd quartile (75th percentile), so it contains the middle half of the data. The line inside is the median; the whiskers extend to the most extreme values within 1.5 IQR, and points beyond are plotted as potential outliers.';text = res1 + res2 + res3;element.innerHTML = text;\n         return false;\n         }}</script>\n```\n\n:::\n:::\n\n\n\n\n\n\n[Back to top](#week7)\n\n\n\n\n\n",
    "supporting": [
      "07-week7_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}